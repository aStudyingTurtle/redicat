//! Deprecated placeholder.
//! The `Bam2MtxProcessor` implementation lived here previously but has been removed to avoid
//! duplication with the CLI chunk processor. This file remains only to preserve repository
//! history; the module is no longer referenced in `mod.rs` and will be deleted in a future
//! cleanup.
